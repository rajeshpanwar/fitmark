var getUserInfo = function (req, res, next) {
    var response = req.session.user;
    var output = "session = " + JSON.stringify(response);
    res.send(output);
};
module.exports = function (parent) {
    parent.get('/user/info.js', getUserInfo);
};